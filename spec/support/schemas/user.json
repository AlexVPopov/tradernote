{
  "type": "object",
  "required": ["user"],
  "properties": {
    "user": {
      "type": "object",
      "required": ["email", "auth_token"],
      "properties": {
        "email": { "type": "string", "format": "email" },
        "auth_token": { "type": "string", "minLength": 32, "maxLength": 32 }
      }
    }
  }
}
